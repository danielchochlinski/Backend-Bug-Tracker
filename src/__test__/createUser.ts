// import { registerUser } from "../controllers/authController";
// import { User } from "../models/userModel";
// import { connectDB, closeDB, clearDB } from "../config/test/dbTest";
// import supertest from "supertest";
// import { app } from "../server";
// beforeAll(async () => await connectDB());
// //
// afterEach(async () => await closeDB());

// afterAll(async () => await clearDB());

// describe("get project route", () => {
//   describe("given the project exists", () => {
//     it("should return a 200 and the project", async () => {
//       const resVar = await supertest(app).get(`/api/test/get-projects`);
//       expect(resVar.statusCode).toBe(200);
//     });
//   });
// });
